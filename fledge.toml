# fledge.toml: aps CLI lifecycle tasks
# Docs: https://github.com/CorvidLabs/fledge

[tasks]
build = "swift build"
test = "swift test"
release-build = "swift build -c release"
smoke = "./Scripts/smoke.sh"
# Keep specsync out of lanes.verify: Trust/SpecSync run it as a top-level gate
# so verification commands cannot recurse into specsync check.
spec = "specsync check"
check = "fledge run build && fledge run test && fledge run smoke && fledge run spec"

[lanes.verify]
description = "Native quality gate (build, test, smoke)"
steps = ["build", "test", "smoke"]
