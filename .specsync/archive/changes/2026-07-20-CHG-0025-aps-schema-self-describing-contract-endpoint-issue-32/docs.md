---
change: CHG-0025-aps-schema-self-describing-contract-endpoint-issue-32
artifact: docs
---

# Docs

README agent section: `aps schema` leads the example block and gets a
paragraph (schemaVersion drift rule, static-contract rule), plus a pointer
to ArgumentParser's `--experimental-dump-help` tree as the free complement.
