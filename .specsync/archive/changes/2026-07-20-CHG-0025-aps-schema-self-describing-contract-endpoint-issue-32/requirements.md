---
change: CHG-0025-aps-schema-self-describing-contract-endpoint-issue-32
artifact: requirements
---

# Requirements

- New REQ-aps-cli-016: `aps schema` emits one cacheable JSON contract
  document (cliVersion, schemaVersion, state-root precedence, keys,
  commands, payload shapes, error table); static contract only.
